// BlockType
export enum BlockType {
  PAGE = 'page',
  TEXT = 'text',
  RICH_TEXT = 'richText',
  IMAGE = 'image',
  VIDEO = 'video',
}

export enum TextLevel {
  H1,
  H2,
  H3,
  H4,
  H5,
  H6,
}
